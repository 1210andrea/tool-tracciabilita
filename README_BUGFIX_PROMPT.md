(Generated by BLACKBOXAI) Note: This repo contains implementation already for top selector and async AI generation.

Pending changes from prompt.md checklist:
- Many-to-many spare_part_types schema + backend routes + frontend AdminPanel spare parts UI (checkbox by types).
- Ensure machines have reparto column (or map from existing machines.type).

